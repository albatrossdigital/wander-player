'use strict';

/* Filters */
/*mapApp.filter('getByLatLng', function() {
  return function(input, id) {
    var i=0, len=input.length;
    for (; i<len; i++) {
      if (+input[i].lat === ) {
        return input[i];
      }
    }
    return null;
  }
});*/